# --- Secrets: must never enter a public repo ---
.env
.env.*
!.env.example
*.key
*.pem
run_scripts/

# --- Foundry ---
out/
cache/
broadcast/
!broadcast/*/*/run-latest.json

# --- Node / subgraph / frontend ---
node_modules/
generated/
build/
dist/
.next/
.pnpm-store/

# --- Isolated workspaces (git worktree) ---
# Without this, an entire worktree gets committed into the repo
.worktrees/
.superpowers/

# --- Misc ---
.DS_Store
*.log

# --- matchstick build artifacts ---
subgraph/tests/.bin/
subgraph/tests/.latest.json

# The reading script. A rendering of docs/the-story.md produced by read-aloud.py --md;
# tracking it would mean two copies that can disagree, and the one anybody edits by hand
# is the one that gets regenerated over.
docs/narration.md
